{
  "name": "gronify",
  "version": "1.0.0",
  "description": "A standalone command-line tool to flatten, search, and unflatten JSON.",
  "keywords": [
    "json",
    "gron",
    "cli",
    "search",
    "platform-engineering",
    "developer-tools"
  ],
  "homepage": "https://github.com/1solomonwakhungu/gronify#readme",
  "bugs": {
    "url": "https://github.com/1solomonwakhungu/gronify/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/1solomonwakhungu/gronify.git",
    "directory": "src/packages/cli"
  },
  "author": "Solomon Wakhungu",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "gronify": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc && node scripts/inject-version.mjs",
    "build:bundle": "npm run build && esbuild dist/index.js --bundle --platform=node --format=esm --target=node20 --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --outfile=dist/gronify",
    "lint": "tsc --noEmit",
    "format:check": "npx --yes prettier@3.6.2 --check package.json",
    "test": "jest",
    "test:watch": "jest --watch",
    "audit": "npm audit --audit-level=moderate"
  },
  "type": "module",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.5.2",
    "esbuild": "^0.25.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.4",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "commander": "^14.0.1"
  }
}
